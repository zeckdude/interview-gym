import { PageWrapper } from '@/components/layout/PageWrapper';
import { SimulatorHistory } from '@/components/simulator/SimulatorHistory';

export default function SimulatorHistoryPage() {
  return (
    <PageWrapper title="Simulator History" fullWidth>
      <SimulatorHistory />
    </PageWrapper>
  );
}
