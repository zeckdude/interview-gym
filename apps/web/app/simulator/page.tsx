import { PageWrapper } from '@/components/layout/PageWrapper';
import { SimulatorSetup } from '@/components/simulator/SimulatorSetup';

export default function SimulatorPage() {
  return (
    <PageWrapper title="Interview Simulator">
      <SimulatorSetup />
    </PageWrapper>
  );
}
