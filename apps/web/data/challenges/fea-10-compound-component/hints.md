1. What data structure tracks which panels are registered and which is expanded?
2. When expand(id) is called, what must happen to all other panels?
3. How do you implement the constraint that only one panel can be expanded at a time?
