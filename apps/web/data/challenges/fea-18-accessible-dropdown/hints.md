1. What index should the dropdown start at when it opens, and what should it be when closed?
2. How do you implement wrapping for navigate() — going past the last item loops to the first?
3. What two things need to happen when selectCurrent() is called?
