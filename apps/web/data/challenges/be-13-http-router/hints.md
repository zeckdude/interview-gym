1. How would you convert a route pattern like '/users/:id' into a regex that matches '/users/42' and captures '42'?
2. When you find a match, how do you extract the param names (':id', ':name') and map them to their captured values?
3. How would you store registered routes so you can efficiently search through them by method and pattern?
