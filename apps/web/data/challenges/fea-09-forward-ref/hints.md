1. What methods should the "ref" object expose so the parent can control the input?
2. How do you separate the ref's API from the component's internal state?
3. What does focus() need to do to the internal state?
