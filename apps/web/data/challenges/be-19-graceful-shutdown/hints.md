1. How would you run a list of async functions one after another (sequentially), not all at once?
2. How do you ensure the shutdown() function can only trigger cleanup once, even if called multiple times?
3. What does the manager need to store between registering handlers and triggering shutdown?
