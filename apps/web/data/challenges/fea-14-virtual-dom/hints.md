1. What is the first thing to check when comparing two nodes — before looking at props or children?
2. How do you detect which props have changed between oldNode.props and newNode.props?
3. What should happen when the node types are different — can you update in place?
