1. How do you prevent the loader from being called twice if load() is called while loading is already in progress?
2. Where do you cache the resolved module so subsequent calls return it instantly?
3. What should load() return when the module is already loaded?
