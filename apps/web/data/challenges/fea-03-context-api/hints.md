1. How do you store the current context value so that useContext() can always read the latest one?
2. What should useContext() return before any Provider sets a value?
3. How do you ensure two different createContext() calls don't share the same value storage?
