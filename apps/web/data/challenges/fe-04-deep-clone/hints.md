1. What are the base cases for your recursion — which types of values should be returned as-is without cloning?
2. How do you tell the difference between an array and a plain object in JavaScript?
3. For each key in an object (or each element in an array), what do you do to deep clone it?
