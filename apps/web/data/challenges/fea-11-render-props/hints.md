1. What data does the render prop function receive, and when is it called?
2. How do you update the state and re-call the render prop after a fetch completes?
3. What's the structural difference between the render props pattern and a custom hook?
