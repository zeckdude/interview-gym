## The Challenge

Implement `validateProductionConfig(config)` ensuring production readiness:

- `output === 'standalone'`
- `headers` includes CSP and HSTS
- `envValidated === true`
- At least one redirect defined