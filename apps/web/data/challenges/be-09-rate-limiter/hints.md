1. What information do you need to store in order to know whether a new call is within the rate limit?
2. How would you determine which previously recorded calls are still "within the current window"?
3. What's the difference between a fixed window and a sliding window rate limiter? Which is simpler to implement first?
