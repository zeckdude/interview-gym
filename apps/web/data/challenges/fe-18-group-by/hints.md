1. What would the accumulated value look like as you process each element with reduce?
2. For each element, what two things do you need to do: compute the group key, then what?
3. What do you do when the key hasn't been seen before (the group array doesn't exist yet)?
