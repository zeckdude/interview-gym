1. A HOC takes a component (function) and returns an enhanced version — what does the enhanced version do differently?
2. How do you pass through all the original props to the wrapped component while also adding behavior?
3. How would you capture log messages in an array instead of printing them to the console?
