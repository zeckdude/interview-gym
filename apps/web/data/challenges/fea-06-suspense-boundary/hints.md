1. What are the three states of an async operation, and how would you track the current one?
2. How do you run an async function and react to whether it resolves or rejects?
3. What should render() return immediately (synchronously) while the async function is running?
