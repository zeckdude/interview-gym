1. How would you catch any error thrown by renderFn() and return the fallback instead?
2. Where do you store the caught error so hasError() and getError() can access it later?
3. What should render() return when renderFn() throws vs. when it succeeds?
