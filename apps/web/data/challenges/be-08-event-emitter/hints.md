1. What data structure would you use to map event names to their list of listeners?
2. For the `once` method, how could you wrap the original listener so it removes itself the first time it's called?
3. When `off` is called, how do you identify which listener to remove from the list?
