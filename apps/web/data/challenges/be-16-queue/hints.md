1. How would you track how many jobs are currently running vs. waiting?
2. When a job finishes, how do you decide whether to start the next job from the queue?
3. How would you wrap a queued job in a Promise so the caller can await its result?
