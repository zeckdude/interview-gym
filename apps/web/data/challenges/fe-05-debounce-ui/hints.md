1. What happens to the current timer when the debounced function is called again before the delay expires?
2. Where do you store the timer ID so that the next call can clear it?
3. When should the original function actually be called?
