export const starterTs = `function heightFromAspectRatio(width: number, ratio: number) {
  // Implement this function
  
}

export { heightFromAspectRatio };`;

export const starterJs = `function heightFromAspectRatio(width, ratio) {
  // Implement this function
  
}

module.exports = { heightFromAspectRatio };`;

export const solutionTs = `function heightFromAspectRatio(width: number, ratio: number) {
  return width / ratio;
}

export { heightFromAspectRatio };`;

export const solutionJs = `function heightFromAspectRatio(width, ratio) {
  return width / ratio;
}

module.exports = { heightFromAspectRatio };`;
