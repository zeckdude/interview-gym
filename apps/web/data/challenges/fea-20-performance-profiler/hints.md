1. What data do you need to store between start(label) and end(label) to compute duration?
2. How do you group measurements by label to compute per-label statistics?
3. What formula gives you the average, min, and max for a set of durations?
