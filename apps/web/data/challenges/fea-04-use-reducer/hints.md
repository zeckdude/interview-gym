1. What two things does a reducer receive, and what must it return?
2. How does dispatch update the state using the reducer?
3. Why must reducers never modify the state object directly — what should they do instead?
