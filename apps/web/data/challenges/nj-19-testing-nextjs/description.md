## The Challenge

Implement `validateTestPlan(plan)` checking a test plan covers required Next.js scenarios.

Required keys: `serverComponent`, `routeHandlerAuth`, `middlewareRedirect`, `clientRouterMock` — all must be true.