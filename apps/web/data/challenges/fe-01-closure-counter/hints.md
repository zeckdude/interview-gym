1. Where would you declare the count variable so that the returned functions can access it, but outside code cannot?
2. How do you remember the initial value so reset() can return to it?
3. What does each method need to do to the count variable?
