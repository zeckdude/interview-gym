1. What does JSON.stringify() produce, and why might that be unsafe inside a <script> tag?
2. How would you escape the string "</script>" so it can safely appear inside a <script>?
3. What data structure lets you store multiple keys of serialized data?
