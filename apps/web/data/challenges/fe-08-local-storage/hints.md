1. What two steps does get() need to do, and what should happen if either step fails?
2. How does set() turn a JavaScript value into something localStorage can store?
3. What should get() return when the key has never been set?
